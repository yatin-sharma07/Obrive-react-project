const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ host: process.env.DB_HOST, port: parseInt(process.env.DB_PORT), database: process.env.DB_NAME, user: process.env.DB_USER, password: process.env.DB_PASSWORD, ssl: { rejectUnauthorized: false } }); (async () => { await pool.query('CREATE TABLE IF NOT EXISTS projects (id SERIAL PRIMARY KEY, project_id VARCHAR(50) UNIQUE NOT NULL, name VARCHAR(255) NOT NULL, description TEXT, priority VARCHAR(20), created_at TIMESTAMP DEFAULT NOW())'); await pool.query('CREATE TABLE IF NOT EXISTS project_assignments (id SERIAL PRIMARY KEY, project_id INT REFERENCES projects(id) ON DELETE CASCADE, employee_id INT REFERENCES users(id) ON DELETE CASCADE)'); console.log('Tables created'); await pool.end(); })();
